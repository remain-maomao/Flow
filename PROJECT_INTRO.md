# Flow — 智能专注力管理桌面应用

## 一句话介绍

Flow 是一款 Windows 桌面应用，通过实时监测你当前使用的窗口和浏览器标签页，自动判断你在工作还是娱乐，并在合适的时机弹出表情包 + 音效提醒你回归专注。

## 解决了什么问题

**场景**：打开电脑本来要工作，不知不觉就点开了 YouTube / B站 / 微博，一刷就是半小时。

**痛点**：
- 番茄钟类工具需要手动启动，经常忘记开
- 网站屏蔽类工具太粗暴，学习频道也被封
- 现有工具缺乏趣味性，提醒烦人、用户关掉就再也不开

**Flow 的做法**：全自动、零操作。它静默运行在系统托盘，识别到你开始摸鱼时，弹出一个萌萌的表情包 + 俏皮音效提醒你。不强制打断，而是用轻松的方式让你自己意识到"该回去了"。

## 核心亮点

### 1. 全自动模式识别（零操作）

- 通过 JNA 调用 Win32 API 获取当前前台窗口标题
- 通过 Chrome 扩展 + WebSocket 获取浏览器当前标签页 URL
- **互斥分类**：浏览器标签页精确匹配优先，窗口标题兜底
- 5 秒防抖机制，避免频繁切换导致误判

### 2. 灵活的黑白名单

- 娱乐域名黑名单：YouTube、B站、微博、抖音等开箱即用，可自定义
- 应用关键词匹配：Steam、游戏等通过窗口标题识别
- URL 白名单：娱乐站的学习频道（如 youtube.com/@freecodecamp）强制识别为工作模式

### 3. 表情包 + 音效 = 不烦人的提醒

- 透明背景弹窗，右下角浮出，像微信表情
- 用户自定义表情包文件夹，每次随机展示
- 用户自定义音效文件夹，每次随机播放
- 纯文字降级模式：没配置表情包也能正常使用

### 4. 智能提醒节奏

- 工作模式：每 15 分钟提醒喝水 💧，每 40 分钟提醒站立 🧍
- 娱乐模式：每 2 分钟轻提醒 ⏰
- 演示加速模式：60x 时间倍速，1 秒 = 1 分钟，方便测试
- 所有参数可通过 Settings 调整

### 5. 工程亮点

- **Bug 复盘**：分类逻辑经历了三轮重构（merge 双流互斥 → 单入口分类 → State 捕获陷阱修复），产出 `CLASSIFIER_SPEC.md` 和 `POSTMORTEM.md`
- **音效音量**：javax.sound.sampled 默认音量 0dB 过低，通过 MASTER_GAIN 推至 6dB 解决
- **格式兼容**：非 PCM WAV 文件自动转换为 PCM_SIGNED 44100Hz 16bit stereo
- **零额外依赖**：音效播放只用 JDK 内置库，不引入第三方音频框架

## 技术栈

| 分类 | 技术 | 用途 |
|------|------|------|
| 语言 | Kotlin 2.0.21 | 全项目 |
| UI | Compose Multiplatform 1.7.1 | Desktop 窗口 + 设置面板 + 弹窗 |
| 构建 | Gradle 8.x + Kotlin DSL | 依赖管理 + 打包 MSI |
| 系统调用 | JNA 5.x | Win32 API：GetForegroundWindow + GetWindowText |
| 浏览器通信 | Chrome Extension (Manifest V3) + WebSocket (Java-WebSocket) | 获取当前标签页 URL + 页面标题 |
| 序列化 | kotlinx.serialization 1.7.3 | 配置持久化 (config.json) |
| 音频 | javax.sound.sampled (JDK 内置) | WAV 播放 + 格式转换 |
| 图片 | Skia (Compose 内置) | 表情包图片加载 (JPG/PNG/JFIF) |

## 架构一览

```
┌──────────────────────────────────────────┐
│                  Flow App                 │
├──────────┬──────────┬──────────┬─────────┤
│ 窗口监测  │ 浏览器扩展 │ 提醒引擎  │ 通知弹窗 │
│  (JNA)   │(WebSocket)│(定时器)  │(表情+音效)│
├──────────┴──────────┴──────────┴─────────┤
│              模式分类器                    │
│     (黑名单 + 白名单 + 5s 防抖)            │
├──────────────────────────────────────────┤
│            系统托盘 + 设置面板              │
└──────────────────────────────────────────┘
```

## 运行截图（文字描述）

> 右下角弹出 120dp 表情包图片 + 半透明黑底白字提醒文字，持续 4 秒自动消失。
> 主窗口 680×500，左侧 Dashboard 显示当前模式、计时、桌面窗口、浏览器标签页、扩展连接状态，右侧 Settings 编辑黑白名单和资源路径。

---

*2026-05 — 个人项目，从零构建，累计 4 个 Phase 迭代。*
